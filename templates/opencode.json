{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "ai-cli": {
      "type": "local",
      "command": ["mise", "exec", "--", "pnpm", "exec", "ai-cli-mcp"],
      "enabled": true,
      "environment": {
        "MCP_CLAUDE_DEBUG": "false"
      }
    }
  }
}
